class Solution {
public:
    int getSum(int a, int b) {
        vector<int> v;
        v.push_back(a);
        v.push_back(b);
        int initial_sum=0;
        return accumulate(v.begin(),v.end(),initial_sum);  // Sum Using accumulate() Library function
    }
};
