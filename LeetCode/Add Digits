class Solution {
public:
    int addDigits(int num) {
        if(num==0) //checking edge case
            return 0;
        int result= (num%9==0)?9:(num%9); //generalizing after seeing pattern
        return result;
    }
};
